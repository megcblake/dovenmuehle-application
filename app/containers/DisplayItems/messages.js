import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DisplayItems';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Review Items',
  },
});
