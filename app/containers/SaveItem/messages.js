import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SaveItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Save Item',
  },
});
